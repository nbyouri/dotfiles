This program, taken from top-73 at opensource.apple.com displays cpu usage in %.

- cpu.c only shows user cpu usage 
- ticks.c shows user, system and idle cpu usage.

Compile this program with:

`clang cpu.c -o cpu`


-- thanks to bottomy.
